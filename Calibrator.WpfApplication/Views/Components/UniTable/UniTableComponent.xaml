<UserControl x:Class="Calibrator.WpfApplication.Views.Components.UniTable.UniTableComponent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:resources="clr-namespace:Calibrator.WpfApplication.Resources"
             xmlns:scButton="clr-namespace:Calibrator.WpfApplication.Views.Components.ScButton"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <!-- ReSharper disable once Xaml.RedundantResource -->
        <DataTemplate x:Key="{x:Static resources:ResourceKeys.UniTableOperationsTemplate}">
            <StackPanel Orientation="Horizontal">
                <ItemsControl
                    ItemsSource="{Binding TableOperations, RelativeSource={RelativeSource AncestorType=UserControl}}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Style="{StaticResource ScRegularButton}"
                                    Command="{Binding OnExecuteActionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    Margin="0 0 10 0"
                                    Background="Transparent">
                                <Button.CommandParameter>
                                    <MultiBinding Converter="{StaticResource UniTableActionEntityConverter}">
                                        <Binding />
                                        <Binding RelativeSource="{RelativeSource AncestorType=telerik:GridViewRow}"
                                                 Path="DataContext" />
                                    </MultiBinding>
                                </Button.CommandParameter>
                                <iconPacks:PackIconMaterial 
                                    Kind="{Binding IconKind}"
                                    Width="20"
                                    Height="20"/>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <telerik:RadGridView
            telerik:StyleManager.Theme="Material"
            x:Name="GridView"
            VerticalAlignment="Stretch"
            HorizontalAlignment="Stretch"
            AutoGenerateColumns="False"
            CanUserDeleteRows="False"
            CanUserInsertRows="False"
            CanUserReorderColumns="false"
            CanUserSortColumns="False"
            IsFilteringAllowed="False"
            RowIndicatorVisibility="Collapsed"
            ShowGroupPanel="False"
            ShowSearchPanel="False"
            ValidatesOnDataErrors="None"
            CanUserFreezeColumns="False"
            IsReadOnly="True">
        </telerik:RadGridView>
    </Grid>
</UserControl>


