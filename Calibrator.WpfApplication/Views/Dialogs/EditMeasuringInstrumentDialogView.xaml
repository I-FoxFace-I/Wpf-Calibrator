<views:BaseView
    Style="{StaticResource BaseViewStyle}"
    x:Class="Calibrator.WpfApplication.Views.Dialogs.EditMeasuringInstrumentDialogView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:vm="clr-namespace:Calibrator.WpfApplication.ViewModels"
    xmlns:scTextBox="clr-namespace:Calibrator.WpfApplication.Views.Components.ScTextBox"
    xmlns:scButton="clr-namespace:Calibrator.WpfApplication.Views.Components.ScButton"
    xmlns:scDropdown="clr-namespace:Calibrator.WpfApplication.Views.Components.ScDropdown"
    xmlns:scDivider="clr-namespace:Calibrator.WpfApplication.Views.Components.ScDivider"
    xmlns:views="clr-namespace:Calibrator.WpfApplication.Views.Base"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance Type=vm:EditMeasuringInstrumentDialogViewModel, IsDesignTimeCreatable=False}"
    Width="730"
    Height="570">
    <Grid>
        <Grid.Resources>
            <Style TargetType="Border">
                <Setter Property="Padding" Value="10 0 0 0" />
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <scDivider:ScDividerComponent Grid.Row="0" Margin="10 30 10 30" Text="General"></scDivider:ScDividerComponent>

        <Border Grid.Row="1">
            <StackPanel Orientation="Horizontal">
                <scTextBox:ScTextBoxComponent
                    Margin="0 0 30 0" LabelText="Name"
                    TextBoxText="{Binding Name}">
                </scTextBox:ScTextBoxComponent>
                <scTextBox:ScTextBoxComponent
                    Margin="0 0 30 0"
                    LabelText="Serial Number"
                    TextBoxText="{Binding SerialNumber}">
                </scTextBox:ScTextBoxComponent>
                <scDropdown:ScDropdownComponent
                    LabelText="Measuring Instrument Type"
                    Items="{Binding AllMeasuringInstrumentTypes}"
                    SelectedItem="{Binding SelectedMeasuringInstrumentType}"
                    DropdownWidth="180">
                </scDropdown:ScDropdownComponent>
            </StackPanel>
        </Border>

        <scDivider:ScDividerComponent Grid.Row="2" Margin="10 30 10 30" Text="Connection settings"></scDivider:ScDividerComponent>

        <Border Grid.Row="3">
            <StackPanel Orientation="Horizontal">
                <scDropdown:ScDropdownComponent
                    LabelText="Connection"
                    Items="{Binding AllConnectionMethods}"
                    SelectedItem="{Binding SelectedConnectionMethod}"
                    DropdownWidth="180">
                </scDropdown:ScDropdownComponent>
            </StackPanel>
        </Border>

        <Border Grid.Row="4">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom"
                        Margin="0 0 0 50">
                <scButton:ScButtonComponent Content="Confirm" ButtonType="Accent" ButtonWidth="100"
                                            Margin="0 0 150 0"
                                            Command="{Binding SaveCommand}" />
                <scButton:ScButtonComponent Content="Cancel" ButtonType="Transparent" ButtonWidth="75"
                                            Command="{Binding CloseCommand}" />
            </StackPanel>
        </Border>
    </Grid>
</views:BaseView>