<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
                    xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
                    xmlns:resources="clr-namespace:Calibrator.WpfApplication.Resources">
    <BooleanToVisibilityConverter x:Key="BooleanToVisibility" />

    <ControlTemplate x:Key="GridViewCheckBoxTemplate" TargetType="telerik:GridViewCheckBox">
        <Grid>
            <Border x:Name="Border"
                BorderThickness="2"
                CornerRadius="3"
                Background="White"
                BorderBrush="Gray"
                Width="24"
                Height="24">
                <Grid>
                    <iconPacks:PackIconMaterial
                    x:Name="CheckMark"
                    Kind="Check"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Visibility="Collapsed"
                    Foreground="White" />
                </Grid>
            </Border>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="Border" Property="Background" Value="#054E5A" />
                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible" />
                <Setter TargetName="Border" Property="BorderThickness" Value="0" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Border" Property="BorderBrush" Value="DarkGray" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style TargetType="telerik:GridViewCell" x:Key="{x:Static resources:ResourceKeys.GridViewCellStyle}">
        <Style.Resources>
            <Style TargetType="telerik:GridViewCheckBox">
                <Setter Property="Template" Value="{StaticResource GridViewCheckBoxTemplate}" />
            </Style>
        </Style.Resources>
    </Style>

    <Style TargetType="Border" x:Key="ShadowedBorder">
        <Setter Property="Background" Value="White" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="Black" Opacity="0.3" BlurRadius="5" ShadowDepth="2" />
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>