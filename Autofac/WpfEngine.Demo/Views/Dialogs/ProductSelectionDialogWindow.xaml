<windows:DialogWindow  xmlns:windows="clr-namespace:WpfEngine.Views.Windows;assembly=WpfEngine"
    x:Class="WpfEngine.Demo.Views.Dialogs.ProductSelectionDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding DisplayName}" 
        Height="600" Width="800"
        WindowStartupLocation="CenterOwner">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" 
                Background="{DynamicResource {x:Static SystemColors.ControlLightBrushKey}}"
                Padding="10">
            <TextBlock Text="Select Product" FontSize="18" FontWeight="Bold"/>
        </Border>
        
        <!-- Filter Bar -->
        <Border Grid.Row="1" Padding="10" Background="#F5F5F5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Search Box -->
                <TextBox Grid.Column="0" 
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,10,0"
                         VerticalAlignment="Center">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Escape" Command="{Binding ClearFiltersCommand}"/>
                    </TextBox.InputBindings>
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TextBox">
                                        <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="3">
                                            <Grid>
                                                <ScrollViewer x:Name="PART_ContentHost"/>
                                                <TextBlock Text="Search products..." 
                                                          Foreground="Gray" 
                                                          Margin="5,0"
                                                          VerticalAlignment="Center"
                                                          IsHitTestVisible="False">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" Value="">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" Value="{x:Null}">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                            <Setter Property="Visibility" Value="Hidden"/>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TextBox.Style>
                </TextBox>
                
                <!-- Category Filter -->
                <ComboBox Grid.Column="1" 
                          SelectedItem="{Binding SelectedCategory}"
                          ItemsSource="{Binding Categories}"
                          Width="120"
                          Margin="0,0,10,0">
                    <ComboBox.Style>
                        <Style TargetType="ComboBox">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedCategory}" Value="{x:Null}">
                                    <Setter Property="Text" Value="All Categories"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ComboBox.Style>
                </ComboBox>
                
                <!-- Show Available Only -->
                <CheckBox Grid.Column="2" 
                          Content="Available Only" 
                          IsChecked="{Binding ShowOnlyAvailable}"
                          VerticalAlignment="Center"
                          Margin="0,0,10,0"/>
                
                <!-- Clear Filters -->
                <Button Grid.Column="3" 
                        Content="Clear Filters" 
                        Command="{Binding ClearFiltersCommand}"
                        Padding="10,5"
                        Margin="0,0,10,0"/>
                
                <!-- Add Custom Product -->
                <Button Grid.Column="4" 
                        Content="Add Custom..." 
                        Command="{Binding AddCustomProductCommand}"
                        Padding="10,5"
                        FontWeight="Bold"/>
            </Grid>
        </Border>
        
        <!-- Product List -->
        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding ProductsView}"
                  SelectedItem="{Binding SelectedProduct}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionMode="Single"
                  Margin="10">
            <DataGrid.InputBindings>
                <MouseBinding MouseAction="LeftDoubleClick" 
                             Command="{Binding SelectCommand}"/>
            </DataGrid.InputBindings>
            
            <DataGrid.Columns>
                <DataGridTextColumn Header="SKU" 
                                   Binding="{Binding SKU}" 
                                   Width="80"/>
                <DataGridTextColumn Header="Name" 
                                   Binding="{Binding Name}" 
                                   Width="200"/>
                <DataGridTextColumn Header="Category" 
                                   Binding="{Binding Category}" 
                                   Width="100"/>
                <DataGridTextColumn Header="Price" 
                                   Binding="{Binding Price, StringFormat=C}" 
                                   Width="80">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextAlignment" Value="Right"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Stock" 
                                   Binding="{Binding Stock}" 
                                   Width="60">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextAlignment" Value="Center"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Stock}" Value="0">
                                    <Setter Property="Foreground" Value="Red"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Description" 
                                   Binding="{Binding Description}" 
                                   Width="*"/>
                
                <!-- Actions -->
                <DataGridTemplateColumn Header="Actions" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Details" 
                                       Command="{Binding DataContext.ShowProductDetailsCommand, 
                                                RelativeSource={RelativeSource AncestorType=Window}}"
                                       CommandParameter="{Binding}"
                                       Margin="2"
                                       Padding="5,2"/>
                                <Button Content="Quick Add" 
                                       Command="{Binding DataContext.QuickAddToOrderCommand, 
                                                RelativeSource={RelativeSource AncestorType=Window}}"
                                       CommandParameter="{Binding}"
                                       Margin="2"
                                       Padding="5,2"
                                       FontWeight="Bold"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
            
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Stock}" Value="0">
                            <Setter Property="Background" Value="#FFF0F0"/>
                        </DataTrigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>
        
        <!-- Selected Product Details -->
        <Border Grid.Row="3" 
                Padding="10" 
                Background="#F9F9F9"
                BorderBrush="LightGray"
                BorderThickness="0,1,0,0"
                Visibility="{Binding SelectedProduct, Converter={StaticResource NullToVisibilityConverter}}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock FontWeight="Bold" FontSize="14">
                        <Run Text="Selected: "/>
                        <Run Text="{Binding SelectedProduct.Name}"/>
                    </TextBlock>
                    <TextBlock Foreground="Gray">
                        <Run Text="Price: "/>
                        <Run Text="{Binding SelectedProduct.Price, StringFormat=C}"/>
                        <Run Text=" | Stock: "/>
                        <Run Text="{Binding SelectedProduct.Stock}"/>
                        <Run Text=" units"/>
                    </TextBlock>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Label Content="Quantity:" VerticalAlignment="Center"/>
                    <TextBox Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" 
                             Width="50" 
                             Margin="0,0,10,0"
                             TextAlignment="Center"
                             VerticalAlignment="Center"/>
                    <Button Content="Details..." 
                            Command="{Binding ShowProductDetailsCommand}"
                            Padding="10,5"
                            Margin="0,0,10,0"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="10">
            <Button Content="Select" 
                    Command="{Binding SelectCommand}"
                    Width="100" 
                    Margin="0,0,10,0"
                    IsDefault="True"
                    FontWeight="Bold"
                    />
            <Button Content="Cancel" 
                    Command="{Binding CancelCommand}"
                    Width="100" 
                    IsCancel="True"/>
        </StackPanel>
        
        <!-- Busy Overlay -->
        <Border Grid.Row="0" Grid.RowSpan="5"
                Background="Black" 
                Opacity="0.5"/>
        
        <Border Grid.Row="0" Grid.RowSpan="5"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Background="White"
                Padding="20"
                CornerRadius="5"
                >
            <StackPanel>
                <ProgressBar IsIndeterminate="True" Width="200" Height="20"/>
                <TextBlock Text="{Binding BusyMessage}" 
                          Margin="0,10,0,0" 
                          HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</windows:DialogWindow >