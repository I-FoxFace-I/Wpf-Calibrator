<windows:ScopedWindow xmlns:windows="clr-namespace:WpfEngine.Views.Windows;assembly=WpfEngine"
                    x:Class="WpfEngine.Demo.Views.Order.Repository.OrderListWindow"
                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    Title="📦 Order Management"
                    Width="1000" Height="700"
                    WindowStartupLocation="CenterScreen"
                    Background="#F8F9FA">

    <windows:ScopedWindow.Resources>
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
    </windows:ScopedWindow.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" 
                Background="#10B981" 
                CornerRadius="8" 
                Padding="20"
                Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="📦 Order Management" 
                           FontSize="24" 
                           FontWeight="Bold" 
                           Foreground="White"/>
                <TextBlock Text="Manage customer orders and shipments" 
                           FontSize="14" 
                           Foreground="#D1FAE5"
                           Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <!-- Action Bar -->
        <StackPanel Grid.Row="1" 
                    Orientation="Horizontal"
                    Margin="0,0,0,16">
            <Button Content="👁️ View Detail" 
                    Command="{Binding ViewDetailCommand}"
                    Style="{StaticResource ActionButton}"
                    Background="#3B82F6"
                    Foreground="White"/>
            <Button Content="🗑️ Delete" 
                    Command="{Binding DeleteOrderCommand}"
                    Style="{StaticResource ActionButton}"
                    Background="#EF4444"
                    Foreground="White"/>
            <Button Content="🔄 Refresh" 
                    Command="{Binding LoadOrdersCommand}"
                    Style="{StaticResource ActionButton}"
                    Background="#10B981"
                    Foreground="White"/>
        </StackPanel>

        <!-- Orders DataGrid -->
        <Border Grid.Row="2" 
                Background="White" 
                CornerRadius="8" 
                BorderBrush="#E2E8F0" 
                BorderThickness="1">
            <DataGrid ItemsSource="{Binding Orders}"
                      SelectedItem="{Binding SelectedOrder}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column"
                      RowHeight="40"
                      CanUserResizeRows="False"
                      SelectionMode="Single"
                      Background="White"
                      BorderThickness="0">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F8FAFC"/>
                        <Setter Property="Foreground" Value="#1E293B"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="12,8"/>
                        <Setter Property="BorderThickness" Value="0,0,0,2"/>
                        <Setter Property="BorderBrush" Value="#E2E8F0"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="12,8"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Padding="{TemplateBinding Padding}" 
                                            Background="{TemplateBinding Background}">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="White"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F1F5F9"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#DBEAFE"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Order #" Binding="{Binding OrderNumber}" Width="120"/>
                    <DataGridTextColumn Header="Customer" Binding="{Binding CustomerName}" Width="*"/>
                    <DataGridTextColumn Header="Date" Binding="{Binding OrderDate, StringFormat='{}{0:dd.MM.yyyy}'}" Width="120"/>
                    <DataGridTextColumn Header="Items" Binding="{Binding Items.Count}" Width="80"/>
                    <DataGridTextColumn Header="Subtotal" Binding="{Binding Subtotal, StringFormat='{}{0:C}'}" Width="120"/>
                    <DataGridTextColumn Header="Tax" Binding="{Binding Tax, StringFormat='{}{0:C}'}" Width="100"/>
                    <DataGridTextColumn Header="Total" Binding="{Binding Total, StringFormat='{}{0:C}'}" Width="120"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                    <DataGridTextColumn Header="Shipping" Binding="{Binding ShippingAddressText}" Width="200"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</windows:ScopedWindow>
