<local:ScopedWindow xmlns:local="clr-namespace:WpfEngine.Demo.Views"
                    x:Class="WpfEngine.Demo.Views.ProductListWindow"
                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    Title="Demo Product List"
                    Width="1000" Height="600"
                    WindowStartupLocation="CenterScreen"
                    Background="#F8F9FA">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="📦 Demo Products (Extended Data)" 
                   FontSize="24" 
                   FontWeight="Bold"
                   Margin="0,0,0,16"/>

        <StackPanel Grid.Row="1" 
                    Orientation="Horizontal"
                    Margin="0,0,0,16">
            <Button Content="👁️ View Detail" 
                    Command="{Binding ViewDetailCommand}"
                    Padding="16,10"
                    Margin="0,0,12,0"/>
            <Button Content="➕ Create New" 
                    Command="{Binding CreateProductCommand}"
                    Padding="16,10"
                    Margin="0,0,12,0"/>
            <Button Content="🗑️ Delete" 
                    Command="{Binding DeleteProductCommand}"
                    Padding="16,10"
                    Margin="0,0,12,0"/>
            <Button Content="🔄 Refresh" 
                    Command="{Binding LoadProductsCommand}"
                    Padding="16,10"/>
        </StackPanel>

        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding Products}"
                  SelectedItem="{Binding SelectedProduct}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Barcode" Binding="{Binding Barcode}" Width="120"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category.Name}" Width="120"/>
                <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat=C2}" Width="100"/>
                <DataGridTextColumn Header="Stock" Binding="{Binding Stock}" Width="80"/>
                <DataGridTextColumn Header="Weight" Binding="{Binding Weight, StringFormat={}{0:F2} kg}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:ScopedWindow>