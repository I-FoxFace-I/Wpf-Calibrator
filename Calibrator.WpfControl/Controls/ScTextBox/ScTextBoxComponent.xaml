<UserControl x:Class="Calibrator.WpfControl.Controls.ScTextBox.ScTextBoxComponent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Label
            Grid.Row="0"
            Content="{Binding LabelText, RelativeSource={RelativeSource AncestorType=UserControl}}"
            Visibility="{Binding Path=IsLabelVisible, Converter={StaticResource BooleanToVisibility}, RelativeSource={RelativeSource AncestorType=UserControl}}"
            Margin="0 0 0 10"/>

        <Border Grid.Row="1"
                Width="{Binding TextBoxWidth, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Height="{Binding TextBoxHeight, RelativeSource={RelativeSource AncestorType=UserControl}}">
            <telerik:RadWatermarkTextBox
                x:Name="TextBoxControl"
                telerik:StyleManager.Theme="Material"
                Style="{StaticResource ScTextBoxStyle}"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                WatermarkContent="{Binding Placeholder, RelativeSource={RelativeSource AncestorType=UserControl}}"
                HorizontalContentAlignment="Left"
                Text="{Binding TextBoxText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                VerticalContentAlignment="Center"
                LostFocus="OnLostFocus"/>
        </Border>
        
        <!-- Validation Error Message -->
        <TextBlock Grid.Row="2"
                   Text="{Binding ValidationError, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   Visibility="{Binding IsValid, Converter={StaticResource InverseBoolToVisibility}}"/>
                                    <!--RelativeSource={RelativeSource AncestorType=UserControl},-->
    </Grid>
</UserControl>
