<UserControl x:Class="Calibrator.WpfControl.Controls.ScCheckBox.ScCheckBoxComponent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d"
             d:DesignHeight="100" d:DesignWidth="300">

    <UserControl.Resources>
        <Style TargetType="CheckBox" x:Key="CustomCheckBoxStyle">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border x:Name="Border" 
                                    Grid.Column="0"
                                    BorderThickness="{StaticResource Calibrator.BorderThickness.Medium}" 
                                    CornerRadius="{StaticResource Calibrator.CornerRadius.Small}" 
                                    Background="{StaticResource Calibrator.Brush.Background}"
                                    BorderBrush="{StaticResource Calibrator.Brush.Border}" 
                                    Width="{StaticResource Calibrator.CheckBox.Size}" 
                                    Height="{StaticResource Calibrator.CheckBox.Size}"
                                    VerticalAlignment="Center">
                                <Grid>
                                    <iconPacks:PackIconMaterial
                                        x:Name="CheckMark"
                                        Kind="Check"
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Stretch"
                                        Visibility="Collapsed"
                                        Foreground="{StaticResource Calibrator.Brush.Background}"/>
                                </Grid>
                            </Border>

                            <ContentPresenter Grid.Column="1"
                                            Margin="10,0,0,0"
                                            VerticalAlignment="Center"
                                            Content="{TemplateBinding Content}"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource Calibrator.Brush.Primary}" />
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible" />
                                <Setter TargetName="Border" Property="BorderThickness" Value="0" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource Calibrator.Brush.BorderDark}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Label -->
        <Label
            Grid.Row="0"
            Style="{StaticResource ScTextBoxLabel}"
            Content="{Binding LabelText, RelativeSource={RelativeSource AncestorType=UserControl}}"
            Visibility="{Binding Path=IsLabelVisible, Converter={StaticResource BooleanToVisibility}, RelativeSource={RelativeSource AncestorType=UserControl}}"
            Margin="0 0 0 10"/>

        <!-- CheckBox -->
        <CheckBox
            x:Name="InnerCheckBox"
            Grid.Row="1"
            HorizontalAlignment="Center"
            Style="{StaticResource CustomCheckBoxStyle}"
            Content="{Binding CheckBoxContent, RelativeSource={RelativeSource AncestorType=UserControl}}"
            IsChecked="{Binding IsChecked, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
            IsEnabled="{Binding IsEnabled, RelativeSource={RelativeSource AncestorType=UserControl}}"
            FontSize="{StaticResource Calibrator.FontSize.Medium}"
            FontFamily="{StaticResource Calibrator.Font.Primary}"
            Foreground="{StaticResource Calibrator.Brush.TextPrimary}"/>
    </Grid>
</UserControl>