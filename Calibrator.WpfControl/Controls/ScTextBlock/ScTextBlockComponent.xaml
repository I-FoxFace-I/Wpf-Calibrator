<UserControl x:Class="Calibrator.WpfControl.Controls.ScTextBlock.ScTextBlockComponent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label
            Grid.Row="0"
            Style="{StaticResource ScTextBoxLabel}"
            Content="{Binding LabelText, RelativeSource={RelativeSource AncestorType=UserControl}}"
            Visibility="{Binding Path=IsLabelVisible, Converter={StaticResource BooleanToVisibility}, RelativeSource={RelativeSource AncestorType=UserControl}}"
            Margin="0 0 0 10"/>

        <Border Grid.Row="1"
                Style="{StaticResource ShadowedBorder}"
                Width="{Binding TextBlockWidth, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Height="{Binding TextBlockHeight, RelativeSource={RelativeSource AncestorType=UserControl}}">
            <telerik:RadWatermarkTextBox
                telerik:StyleManager.Theme="Material"
                Style="{StaticResource ScTextBoxStyle}"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                Text="{Binding Text, RelativeSource={RelativeSource AncestorType=UserControl}}"
                IsReadOnly="True"
                IsTabStop="False"
                Focusable="False"
                Cursor="Arrow"
                HorizontalContentAlignment="Left"
                VerticalContentAlignment="Center"
                TextWrapping="{Binding TextWrapping, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Background="{StaticResource Calibrator.Brush.Surface}"/>
        </Border>
    </Grid>
</UserControl>