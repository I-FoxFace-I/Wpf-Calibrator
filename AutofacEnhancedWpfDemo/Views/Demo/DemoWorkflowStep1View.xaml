<UserControl x:Class="AutofacEnhancedWpfDemo.Views.Demo.DemoWorkflowStep1View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="📋 Step 1: Select Customer" 
                   FontSize="20" 
                   FontWeight="Bold"
                   Margin="0,0,0,16"/>

        <!-- Instructions -->
        <Border Grid.Row="1" 
                Background="#F0F9FF" 
                CornerRadius="8" 
                Padding="16"
                Margin="0,0,0,16">
            <TextBlock Text="Select a customer to create an order for. You can view customer details in a separate window."
                       TextWrapping="Wrap"
                       Foreground="#0369A1"/>
        </Border>

        <!-- Customer List -->
        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding Customers}"
                  SelectedItem="{Binding SelectedCustomer}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="100"/>
                <DataGridTemplateColumn Header="Actions" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="👁️ View Detail" 
                                    Command="{Binding DataContext.ViewCustomerDetailCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding}"
                                    Padding="8,4"
                                    FontSize="11"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Navigation -->
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Content="Next ▶" 
                    Command="{Binding NextCommand}"
                    Padding="20,12"
                    Background="#10B981"
                    Foreground="White"
                    FontWeight="SemiBold"
                    IsEnabled="{Binding SelectedCustomer, Converter={StaticResource NotNullToBoolConverter}}"/>
        </StackPanel>
    </Grid>
</UserControl>