<UserControl x:Class="Calibrator.WpfControl.Sandbox.Views.UniFormDemo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:scButton="clr-namespace:Calibrator.WpfControl.Controls.ScButton;assembly=Calibrator.WpfControl"
             xmlns:scSmartTable="clr-namespace:Calibrator.WpfControl.Controls.ScSmartTable;assembly=Calibrator.WpfControl"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="UniForm Demo - Equipment Management" 
                   FontSize="{StaticResource Calibrator.FontSize.XLarge}"
                   FontWeight="Bold"
                   Margin="0 0 0 20"/>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="1" 
                    Orientation="Horizontal" 
                    Margin="0 0 0 20">
            <scButton:ScButtonComponent
                Content="Add New Equipment"
                ButtonType="Accent"
                IconKind="Plus"
                Command="{Binding OpenAddDialogCommand}"
                Margin="0 0 10 0"/>

            <scButton:ScButtonComponent
                Content="Refresh"
                ButtonType="Regular"
                IconKind="Refresh"
                Command="{Binding RefreshCommand}"/>
        </StackPanel>

        <!-- Equipment List Table -->
        <scSmartTable:ScSmartTableComponent 
            Grid.Row="2"
            Columns="{Binding Columns}"
            ItemsSource="{Binding Equipment}"
            TableOperations="{Binding TableOperations}"
            EnableFiltering="True"
            EnableSorting="True"
            ShowSearchPanel="True"/>
    </Grid>
</UserControl>